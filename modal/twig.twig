<div class="modalify rounded form-modal">

	<header>
		<i class="inverse fal fa-user"></i>
		<label>
			<button (close)><i class="fas fa-times"></i></button>
		</label>
		<nav>
		</nav>
	</header>

	<section>
		Hello {{ tag }}
	</section>

	<footer>
		<button (close) class="primary">Cancel</button>
		<button (ok) class="success">Ok</button>
	</footer>

</div>